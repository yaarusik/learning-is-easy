import { counterReducer } from './model/slice/counterSlice';
import { Counter } from './ui';
import type { ICounterSchema } from './model/types/conterSchema';

export {
    counterReducer,
    Counter,
    ICounterSchema,
};
